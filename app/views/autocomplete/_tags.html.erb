<% tags.each do |tag| %> 
  <li class="list-group-item hover:bg-amber-100 text-stone-400 hover:text-indigo-800 px-4 py-2 rounded-md transition-colors duration-200 ease-in-out" role="option" data-autocomplete-value="<%= tag.id %>">
    <%= tag.name %>
  </li>
<% end %>
