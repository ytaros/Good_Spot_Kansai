<% categories.each do |category| %>
  <li class="list-group-item hover:bg-pink-100 text-stone-400 hover:text-indigo-800 px-4 py-2 rounded-md transition-colors duration-200 ease-in-out" role="option" data-autocomplete-value="<%= category.id %>">
    <%= category.name %>
  </li>
<% end %>