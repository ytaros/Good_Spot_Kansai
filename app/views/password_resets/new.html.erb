<% content_for(:title, t('.title')) %>

<div class="min-h-screen flex items-center justify-center bg-gray-50 p-4">
  <div class="bg-white p-8 rounded-lg shadow-md w-full md:w-1/2 lg:w-1/3 -mt-16">
    <h1 class="text-2xl font-bold mb-6"><%= t('.title') %></h1>
    <%= form_with url: password_resets_path, method: :post, local: true, data: { turbo: false } do |f| %>
      <div class="mb-4">
        <%= f.label :email, User.human_attribute_name(:email), class: "block text-sm font-medium text-gray-600" %>
        <%= f.email_field :email, class: 'mt-1 p-2 w-full border border-purple-500 rounded-md' %>
      </div>
      <div class="flex flex-col items-center justify-between mb-4">
        <%= f.submit t('defaults.send'), class: 'bg-purple-300 hover:bg-purple-400 text-white font-medium py-2 px-4 rounded mb-2' %>
      </div>
    <% end %>
  </div>
</div>
