<!DOCTYPE html>
<html>
  <head>
    <title>GoodSpotKansai</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "https://fonts.googleapis.com/earlyaccess/hannari.css", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://fonts.googleapis.com/css2?family=Nothing+You+Could+Do&display=swap", media: "all" %>


  </head>

  <body>
  <% unless request.path == root_path %>
      <%= render 'shared/header' %>
  <% end %>
  <div class="main-content mt-15 md:mt-20 lg:mt-12">
  <% flash.each do |type, message| %>
    <div data-controller="flash" class="<%= flash_class(type) %> animate-fadeInOut fixed top-15 w-full">
      <%= message %>
    </div>
  <% end %>
    <%= yield %>
  </div>
  <% unless request.path == root_path %>
    <%= render 'shared/footer' %>
  <% end %>
</body>